# TypeScript to MoonBit Tool (Fork of TypeScript to MoonBit Tool)

## Developing

Install [Deno](https://deno.com).

```
# install packages
deno install

# run locally
deno task dev

# run unit tests
deno task test
```